// eslint-disable-next-line import/no-unresolved
import { log, error, warn, info, debug } from './services/FirebaseLogger';

/**
 * functions logger
 */
export const logger = {
  log,
  error,
  warn,
  info,
  debug,
};
